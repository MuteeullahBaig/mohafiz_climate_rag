# Serving-only deps for the HF Spaces container (excludes docling/ragas/wandb, which
# are ingestion/eval-time only — keeps the image lean). Retrieval index is read from
# Qdrant Cloud (QDRANT_URL), so no local Qdrant/data is bundled.
FlagEmbedding
qdrant-client
langgraph
langchain-core<1
groq
fastapi
uvicorn[standard]
gradio
slowapi
sse-starlette
httpx
python-dotenv
