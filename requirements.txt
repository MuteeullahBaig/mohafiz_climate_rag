# Runtime deps — what the HF Gradio Space (and any fresh clone) needs to RUN the app.
# Ingestion/eval extras (docling, ragas, wandb) live in requirements-dev.txt.
FlagEmbedding
qdrant-client
langgraph
langchain-core<1
groq
gradio==6.20.0
httpx
python-dotenv
# FastAPI JSON+SSE variant (api/main.py) — harmless on the Gradio Space, needed for self-host
fastapi
uvicorn[standard]
slowapi
sse-starlette
# ZeroGPU (HF free tier) requires the spaces package + a @spaces.GPU function
spaces
