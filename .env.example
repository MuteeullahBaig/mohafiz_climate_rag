GROQ_API_KEY=
GEMINI_API_KEY=
WANDB_API_KEY=
# Optional: ReliefWeb V2 sitreps tool. Request an approved appname at
# https://apidoc.reliefweb.int/parameters#appname (free). Without it, the
# sitreps tool degrades gracefully and the other 3 live tools still work.
RELIEFWEB_APPNAME=
